# Onboarding feature

Future onboarding screens, components, state, and tests belong in this feature.
Keep onboarding isolated from `features/home`; the navigation layer in
`src/navigation` owns the hand-off from onboarding to the authenticated app.
